type Props = {
    title: string;
    type: string;
    img: string;
    link: string;
    github: string;
}

export default function ProjectDetails(props: Props) {
    return (
        <article>
            
        </article>
    );
}